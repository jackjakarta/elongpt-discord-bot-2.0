# Dev/build tooling (compiled into requirements-dev.txt via pip-compile).
# -c keeps shared deps consistent with the runtime lockfile.
# Not installed in the production Docker image.
-c requirements.txt
black==24.10.0
isort==5.13.2
flake8==7.1.1
watchdog==6.0.0
pip-tools==7.5.3
