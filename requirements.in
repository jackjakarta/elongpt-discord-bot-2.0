# Runtime dependencies (compiled into requirements.txt via pip-compile).
# Pinned to current versions so recompiling prunes tooling without upgrading the bot.
discord.py==2.4.0
openai==2.28.0
httpx==0.27.2
pydantic==2.9.2
python-decouple==3.8
aioboto3==15.5.0
